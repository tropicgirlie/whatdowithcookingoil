name = "oilcycle-worker"
main = "src/index.ts"
compatibility_date = "2024-12-18"

# Update the id after running:  wrangler kv namespace create OILCYCLE
kv_namespaces = [
  { binding = "OILCYCLE", id = "REPLACE_ME_AFTER_KV_CREATE" }
]

[vars]
FRONTEND_URL = "http://localhost:5173"
